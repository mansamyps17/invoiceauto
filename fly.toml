app = "invoiceauto"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[http_service]
  auto_start_machines = true
  auto_stop_machines = false
  force_https = true
  internal_port = 10000
  min_machines_running = 1
  processes = ["app"]
